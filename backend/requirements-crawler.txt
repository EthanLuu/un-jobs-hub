# Minimal dependencies for GitHub Actions crawler workflow
# Only includes what's needed for Playwright-based crawlers

# FastAPI & Web Framework (needed for app structure)
fastapi==0.109.0
python-dotenv==1.0.0

# Database (use psycopg3 for better compatibility)
sqlalchemy==2.0.25
psycopg[binary]==3.1.18
psycopg2-binary==2.9.9

# Web Scraping (only Playwright, no Scrapy)
playwright==1.41.0
beautifulsoup4==4.12.3
lxml==5.1.0

# HTTP & Utilities
httpx==0.26.0

# Validation
pydantic==2.10.5
pydantic-settings==2.7.0
